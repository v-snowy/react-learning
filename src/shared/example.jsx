import { hot } from 'react-hot-loader/root';
import React from 'react';

function ExampleComponent() {
  return (
    <strong>Hello React</strong>
  );
}

export const Example = hot(ExampleComponent);